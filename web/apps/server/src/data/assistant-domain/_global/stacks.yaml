# Named stack recipes: a base source plus the addons that normally go with it.
#
# Ships empty. A recipe is only shown to the assistant when its base source is
# synced in this workspace, so adding one here never affects unrelated plans.
#
# stacks:
#   example_kit_r2:
#     label: Example Kit R2
#     base_source: Example-Printer
#     base_tag: v2.1
#     addon_sources: [Example-Toolhead, Example-Probe]
#     default_selections:
#       toolhead: example_toolhead
stacks: {}
