# Phrase aliases → exact sources.
#
# Ships empty: which phrases mean which source is specific to what you build.
# Add entries here, or in `<PRINT_PARTNER_DATA_DIR>/assistant-domain/_global/`,
# and the assistant will resolve those phrases instead of guessing.
#
# aliases:
#   - phrases: ["the big one", "300 build"]
#     resolve:
#       source_name: Example-Printer
#       tag: v2.1
#       addons: [Example-Toolhead]
#       notes: Optional one-line rationale shown to the model.
aliases: []
