# Files that collide when two sources are stacked, and how to resolve them.
#
# Ships empty — overlaps depend entirely on which repos you layer.
#
# conflicts:
#   - slug_or_path: path/to/part.stl
#     sources: [Example-Printer, Example-Toolhead]
#     resolution: Prefer the toolhead copy; the base copy predates the mod.
conflicts: []
