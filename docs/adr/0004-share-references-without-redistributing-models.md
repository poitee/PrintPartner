# Share references without redistributing models

Build and Library sharing distributes recipes and original-source references, never model files, even when redistribution would be licensed. Recipients acquire models independently using their own access. This preserves creator distribution channels and keeps Git sharing separate from model hosting, backups, printer control, and production progress.
